[project]
name = "witching-hour-qiskit"
version = "0.1.0"
description = "Minimal qBraid and Qiskit repository for ecosystem submission."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "qbraid[qiskit]>=0.11,<0.12",
]

[tool.setuptools]
py-modules = []
